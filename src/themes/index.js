import { createTheme } from "@mui/material";

const themeForm = createTheme({
    palette: {
        primary: {
            main: "#fff"
        },
        secondary: {
            main: "#000"
        }
    }
})

export { themeForm }